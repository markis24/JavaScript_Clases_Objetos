class cicle {
    constructor(num, nom, numEdicions,data,moduls) {
        this.num = num;
        this.nom = nom;
        // Valor inicial de numEdicions = 0
        this.numEdicions = 0;
        this.data = data;
        this.moduls = [];
    }
}

toString(); {
    return `MP${this.num}. ${this.nom} ${this.moduls} ${this.data}`;
}

/*

setNumEdicions (){
    
}
*/